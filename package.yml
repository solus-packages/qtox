name       : qtox
version    : 1.10.0
release    : 17
source     :
    - https://github.com/qTox/qTox/archive/v1.10.0.tar.gz : a2873c475f56efae268903fd268006cea0771445d572b658ef2fb9e65c121a5d
license    : GPL-3.0
component  : network.im
summary    : qTox is a powerful Tox client.
description: |
    qTox is a powerful Tox client.
builddeps  :
    - pkgconfig(filteraudio)
    - pkgconfig(gdk-pixbuf-2.0)
    - pkgconfig(gtk+-2.0)
    - pkgconfig(libavcodec)
    - pkgconfig(libqrencode)
    - pkgconfig(libsodium)
    - pkgconfig(libtoxcore)
    - pkgconfig(openal)
    - pkgconfig(opencv)
    - pkgconfig(Qt5Help)
    - pkgconfig(Qt5Svg)
    - pkgconfig(sqlcipher)
    - pkgconfig(vpx)
    - pkgconfig(xscrnsaver)
setup      : |
    sed -i -e 's|AudioVideo;||g' qtox.desktop
build      : |
    install -d build
    cd build
    qmake ENABLE_SYSTRAY_UNITY_BACKEND=NO ..
    %make
install    : |
    install -Dm755 build/qtox $installdir/usr/bin/qtox
    install -Dm644 qtox.desktop $installdir/usr/share/applications/qtox.desktop
    cd img/icons
    rm qtox*
    for size in *x*; do
        install -Dm644 "$size/qtox.png" "$installdir/usr/share/icons/hicolor/$size/apps/qtox.png"
    done
